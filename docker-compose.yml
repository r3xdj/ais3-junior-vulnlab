services:
  apache:
    build:
      context: ./ingress
    container_name: ctf-apache
    environment:
      SSH_PIVOT_PASSWORD: ${SSH_PIVOT_PASSWORD}
    ports:
      - "8080:80"
      - "2222:22"
    networks:
      - public_net
      - internal_net
    depends_on:
      - web-gateway
      - frontend

  postgres:
    build: ./datastores/postgres
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - ./datastores/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
      - pgdata:/var/lib/postgresql/data
    networks:
      - internal_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    # 刻意不設密碼,作為 Stage 3 SSRF 打進來後可直接操作的目標
    networks:
      - internal_net
    # 絕對不對外暴露 port,選手必須靠 SSRF 才能碰到

  web-gateway:
    build: ./web-gateway
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CERTIFICATE_DIR: /certificates
    volumes:
      - certificates:/certificates
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - internal_net
      - public_net

  frontend:
    build: ./frontend
    environment:
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
    networks:
      - public_net
    depends_on:
      - web-gateway

  celery-worker:
    build: ./celery-worker
    restart: unless-stopped
    command: celery -A ais3_tasks worker --loglevel=INFO
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      SSH_PIVOT_PASSWORD: ${SSH_PIVOT_PASSWORD}
      CERTIFICATE_DIR: /certificates
    depends_on:
      - redis
      - postgres
    volumes:
      - certificates:/certificates
      - ./flags:/flags:ro   # flags 掛進來,但權限在 Dockerfile 裡控制
    networks:
      - internal_net

networks:
  public_net:
    driver: bridge
  internal_net:
    internal: true

volumes:
  pgdata:
  certificates:
